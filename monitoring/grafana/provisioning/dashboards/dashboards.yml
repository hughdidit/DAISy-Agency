# Grafana dashboard provisioning
apiVersion: 1

providers:
  - name: DAISy Monitoring
    orgId: 1
    folder: DAISy
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
